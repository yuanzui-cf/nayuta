---
interface Props {
  class?: string;
  href?: string;
}

const { class: className = '', href } = Astro.props;
const Element = href ? 'a' : 'span';
---

<Element href={href} class={`tag-item ${className}`.trim()}>
  <slot />
</Element>
